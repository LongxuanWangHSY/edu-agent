# euclid_maths_writing — 写作模块

本模块是**从「欧几里德之门」数学公众号整体迁移过来的内容生产工作流与产出**，作为新项目的参考资产。

架构文档 §1.3 把它列为「**教案模块的雏形**」。它已经在一线跑了大半年、产出 34 篇文章，是一套**被真实使用验证过、也踩过坑**的教学内容生产流程——这比从零设计一套教案生成流程可靠得多。

---

## 目录

```
euclid_maths_writing/
├── README.md              本文件
├── skills/                工作流定义（8 个技能）
└── articles/              产出：34 篇文章 + 52 张配图 + 索引
    ├── No*.md             No 系列长文（22 篇）
    ├── S*.md              S 系列日常短文（8 篇）
    ├── generated_images/  配图
    └── article_index.json 文章索引（含未完成任务追踪）
```

---

## ⚠️ 先读这一条：这些不是给人读的文档

`skills/` 下的 `SKILL.md` 是**写给 AI agent 的执行指令**，不是项目文档。

- 开头的 frontmatter（`name` / `description` / 触发词）是**机器用的**——AI 靠它判断"什么时候该加载这个技能"
- 正文是**祈使句规范**——"必须""禁止""首先…然后…"，读起来像规章手册，这是正常的
- 里面的"用户"指的是 AI 的使用者（即提出写作要求的人），不是本项目的用户

**所以请不要把它们当成产品需求文档来读。** 把它们当成"一套已经在用的 prompt 工程规范"来看——我们要讨论的是**这套流程的哪些部分值得搬进新架构**。

`articles/` 里的文章则是**正常的人类文档**，可以按普通文章读。

---

## 建议阅读顺序

### 如果你只有 15 分钟

| 顺序 | 看什么 | 为什么 |
|---|---|---|
| 1 | `skills/euclids-gate-math-writing/SKILL.md` 的**精神纲领**与**写作宗旨** | 整套工作流的价值观内核——决定了所有模板和规范为什么长这样 |
| 2 | `skills/euclids-gate-writing-interaction/SKILL.md` 的**两阶段流程** | 规划 → 硬门控 → 执行，最值得搬进新架构的流程设计 |
| 3 | 本文件下方的「映射到新架构」 | 看看每个组件在新架构里对应什么 |

### 如果你想看产出长什么样

| 看哪篇 | 看什么 |
|---|---|
| `articles/No23_最少需要几次.md` | **格式最完整的一篇**：题目引用块、分析/解答分层、证明结束符、表格化编码方案 |
| `articles/No20_圆幂定理与根轴.md` | **讲义型长文的代表**：定理统一 → 例题六级递进 → 识别信号速查表。配图最多（10 张） |
| `articles/No17_极端原理.md` + `articles/公告_关于极端原理一处错误的致歉.md` | **说明这套流程的核查规则从哪来**——此文写入过错误的几何命题，被读者发现后公开更正 |
| `articles/S6_一笔画.md` | **日常短文格式**：一题一解一谈，五分钟读完 |

---

## 八个技能是什么

| 技能 | 一句话 | 类型 |
|---|---|---|
| `euclids-gate-math-writing` | 写作引擎：精神纲领、写作宗旨、四类文章模板、语言规范、教学法规范六条 | **核心** |
| `euclids-gate-writing-interaction` | 交互协议：两阶段门控、选题候选制、四条交互原则 | **核心** |
| `euclids-gate-manage` | 文章索引读写、文件保存、版本备份规则 | 基础设施 |
| `euclids-gate-agenda` | 跨会话任务追踪（未完成的思考题、待写专题） | 状态管理 |
| `euclids-gate-teacher-review` | 模拟一线资深教师审阅已完成文章 | **审阅层** |
| `euclids-gate-student-review` | 模拟重点中学学生审阅，反馈真实阅读体验 | **审阅层** |
| `euclids-gate-figure` | 数学配图引擎：图型路由、编译管线、出图质检 | 渲染能力 |
| `humanizer-zh` | 去除 AI 写作痕迹（第三方技能，见其 LICENSE） | 第三方 |

---

## 映射到新架构

**这一节是本文档的重点。** 哪些能力可以变成新架构的原语，哪些只是这个内容项目的运营规则。

### 可迁移（真正的教学能力）

| 现有组件 | 新架构中的位置 | 迁移价值 |
|---|---|---|
| 精神纲领六条 + 写作宗旨 | pack 层的**风格与价值观约束** | 高——这是"什么样的教学是好教学"的显式表达 |
| 教学法规范六条（变式练习、元认知展示、设问留白、金句节制、名言去重、认知负荷路标） | `gen` 原语的**领域知识** | **很高**——可迁移的教学法原理，与具体学科无关 |
| 两阶段门控（规划 → 批准 → 执行） | L3 工作流的**编排协议** | **很高**——任何"AI 生成教学内容"的场景都需要这个防护 |
| 选题候选制（先给 2~3 个候选，用户选） | L3 交互模式 | 高——避免 AI 单方面决定内容方向 |
| teacher-review / student-review | **persona 服务** | 高——现成的多视角审阅机制，可直接复用于题目与教案 |
| 配图引擎（图型路由 + 质检清单） | `render` 原语的数学部分 | 高——含 TikZ/pgfplots/draw.io 三条管线 |
| 命题正确性核查（反例构造、双向检验） | `item.verifiers` 字段的**思想来源** | **很高**——见架构文档 §2.5，这是本产品的核心资产 |

### 项目特有（不要直接搬）

| 现有组件 | 为什么不搬 |
|---|---|
| 公众号编号体系（No 系列 / S 系列） | 内容运营规则，与教学能力无关 |
| 板块轮换、轨道轮换（代数/几何/组合/数论交替） | 同上，是连载策略 |
| 文章长度与节奏规范（5 分钟读完等） | 面向公众号阅读场景，不是教学场景 |
| `article_index.json` 的具体结构 | 是那个项目的存储实现，新架构的 `item` schema 要重新设计 |
| `archive/` 备份与版本号规则 | 内容项目的资产管理约定，非通用需求 |

### 需要重新设计的

| 现有组件 | 问题 |
|---|---|
| `euclids-gate-manage` 的索引机制 | 用 JSON 文件 + 文件系统路径管理文章。新架构的 `store` 原语需要真正的数据层，不能沿用 |
| `euclids-gate-agenda` 的任务追踪 | 目前是 JSON 里的待办列表。若要支撑"学情追踪"，需要换成正式的调度模型（对应架构文档 §5.3 `mastery`） |
| 固定目录结构的假设 | 现有技能假设了"文章在根目录、图在 generated_images/"，新架构不能有这种全局约定 |

---

## 一个需要留意的数据问题

**文章编号在仓库与发布版本之间不一致。**

仓库里的文件名编号是 `No1`–`No23`（**缺 No6**），但公众号后台的实际发布编号与之不符。已核实的两个例子：

| 仓库文件 | 仓库内标题 | 公众号实际发布 |
|---|---|---|
| `No9_二次剩余.md` | No9. 二次剩余的奥秘 | **No11.** 二次剩余的奥秘——从 Legendre 符号到二次互反律 |
| `No11_韦达定理.md` | No11. 韦达定理：对称魔法的代数密码 | **No12.** 韦达定理：对称魔法的代数密码 |

这不是固定偏移（一个 +2、一个 +1），成因待查——可能是发布顺序与仓库编号编排不同步，也可能存在未入库的文章。

**本模块按原样迁移，未做修正**，因为不清楚正确的对应关系。**阅读时以文件内容为准，不要依赖编号顺序。** 该问题需回源项目核对后再处理。

---

## 迁移范围说明

本模块于 2026-09-17 从 `euclid_maths` 仓库整体拷贝：

- **包含**：8 个技能、34 篇文章、52 张配图、文章索引
- **未包含**：`archive/`（技能与文章的历史版本备份）、`devlog/`（项目内部评估记录）、`manim_env/`（动画环境）、含 API 密钥的配置文件（**密钥永不入库**）
- `articles/article_index.json` 中的文件路径指向**原项目位置**（`DeskBox\CoreProjects\euclid_maths`），在本模块内是失效的——它的价值在于展示索引结构，供设计新架构的 `store` 时参考
